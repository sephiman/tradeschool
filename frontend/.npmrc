# TypeScript 7 is newer than some packages' declared peer ranges (e.g. react-i18next
# still lists typescript "^5 || ^6"). TS there is an optional type-only peer, so 7.x
# works fine — accept the broader tree instead of failing the install.
legacy-peer-deps=true
